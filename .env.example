# ── Prescription AI — frontend env ─────────────────────────────
# Base URL of your backend API. Defaults to `/api` (same origin).
VITE_API_BASE_URL=/api

# When "false", axios talks to the real backend.
# When omitted or any other value, the built-in mock adapter handles calls.
VITE_USE_MOCK=true
